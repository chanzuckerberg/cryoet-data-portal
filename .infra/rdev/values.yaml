stack:
  services:
    frontend:
      image:
        tag: sha-dcbae01
      replicaCount: 1
      env:
        - name: API_URL_V2
          value: https://graphql.cryoet.staging.si.czi.technology/graphql
        - name: ENV
          value: dev
      livenessProbe:
        httpGet:
          path: /healthz
