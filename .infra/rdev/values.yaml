stack:
  services:
    frontend:
      image:
        tag: sha-c72ed6b
      replicaCount: 1
      env:
        - name: API_URL_V2
          value: https://graphql.cryoet.staging.si.czi.technology/graphql
        - name: ENV
          value: dev
      livenessProbe:
        httpGet:
          path: /healthz
