stack:
  services:
    frontend:
      image:
        tag: sha-2fde9cd
      ingress:
        enabled: true
      gateway:
        dnsOwner: gateway
      replicaCount: 1
      env:
        - name: API_URL_V2
          value: https://graphql.cryoet.staging.si.czi.technology/graphql
        - name: ENV
          value: staging
      livenessProbe:
        httpGet:
          path: /healthz
