version: "3.8"
services:
  frontend:
    image: frontend
    platform: linux/arm64
    profiles:
    - '*'
    build:
      context: .
      dockerfile: packages/data-portal/Dockerfile
    ports:
    - 8081:8080
